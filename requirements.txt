# Python packages the running server needs.
#
# Installed into the production image, so keep this to what the server
# actually invokes: ytmusicapi is imported by the search helper it spawns,
# and yt-dlp is run as a command. Transitive dependencies come from pip.
#
# ESP32 build and debugging tools belong in requirements-esp32.txt — they are
# never invoked by the server and only inflate the image.
ytmusicapi
yt-dlp
