---
# MusicBox Configuration Sync Playbook
# Only syncs audio/boot configuration (may trigger reboot)

- name: Sync MusicBox configuration
  hosts: musicbox_devices
  gather_facts: yes

  roles:
    - role: musicbox-config
      tags: [config, sync]
