---
# MusicBox Player Deployment Playbook
# Only updates the player application (fast deployment)

- name: Deploy MusicBox player
  hosts: musicbox_devices
  gather_facts: no

  roles:
    - role: musicbox-player
      tags: [player, deploy]
