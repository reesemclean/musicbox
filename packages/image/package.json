{
  "name": "musicbox-image-builder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:image": "node --experimental-transform-types build-image.ts",
    "build:image:interactive": "node --experimental-transform-types build-image.ts --interactive",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.10.2",
    "eslint": "^9.0.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.0.0"
  }
}
