[tools]
node = "24"
python = "3.13"

[env]
_.python.venv = { path = ".venv", create = true }

[tasks.setup]
description = "Install Python dependencies"
run = "pip install -q -r requirements.txt && pip install -q platformio"
